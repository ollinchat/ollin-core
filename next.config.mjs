/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Ensure the app runs correctly on localhost
  // (no rewrites/redirects that would block the dev server)
};

export default nextConfig;
